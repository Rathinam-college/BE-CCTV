version: '3.8'

services:
  backend:
    build: .
    ports:
      - "5000:5000"
    environment:
      # Use the local db.sqlite3 file instead of PostgreSQL
      - USE_SQLITE=True
    volumes:
      - .:/app
